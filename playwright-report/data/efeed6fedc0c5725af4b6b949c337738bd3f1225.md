# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: e2e\auth.setup.ts >> authenticate as User A
- Location: tests\e2e\auth.setup.ts:43:6

# Error details

```
TimeoutError: locator.fill: Timeout 10000ms exceeded.
Call log:
  - waiting for getByLabel(/email/i)

```

# Page snapshot

```yaml
- generic [active] [ref=e1]:
  - alert [ref=e2]
  - generic [ref=e4]:
    - heading "404" [level=1] [ref=e5]
    - heading "This page could not be found." [level=2] [ref=e7]
```

# Test source

```ts
  1  | import { expect, test as setup } from "@playwright/test";
  2  | import fs from "node:fs";
  3  | import path from "node:path";
  4  | 
  5  | const BASE_URL = process.env.BASE_URL ?? "http://localhost:3000";
  6  | const USER_A_EMAIL = process.env.TEST_USER_A_EMAIL ?? "usera@atelier.test";
  7  | const USER_A_PASS = process.env.TEST_USER_A_PASSWORD ?? "TestPassword1!";
  8  | const USER_B_EMAIL = process.env.TEST_USER_B_EMAIL ?? "userb@atelier.test";
  9  | const USER_B_PASS = process.env.TEST_USER_B_PASSWORD ?? "TestPassword2!";
  10 | 
  11 | const AUTH_DIR = path.resolve(__dirname, ".auth");
  12 | 
  13 | setup.beforeAll(() => {
  14 |   if (!fs.existsSync(AUTH_DIR)) {
  15 |     fs.mkdirSync(AUTH_DIR, { recursive: true });
  16 |   }
  17 | });
  18 | 
  19 | async function loginAndSave(
  20 |   email: string,
  21 |   password: string,
  22 |   storageStatePath: string,
  23 |   page: import("@playwright/test").Page,
  24 | ) {
  25 |   await page.goto(`${BASE_URL}/login`);
> 26 |   await page.getByLabel(/email/i).fill(email);
     |                                   ^ TimeoutError: locator.fill: Timeout 10000ms exceeded.
  27 |   await page.getByLabel(/password/i).fill(password);
  28 |   await page.getByRole("button", { name: /sign in|log in|login/i }).click();
  29 |   await page.waitForURL((url) => !url.pathname.includes("/login"), { timeout: 15_000 });
  30 | 
  31 |   expect(page.url()).not.toContain("/login");
  32 |   await page.context().storageState({ path: storageStatePath });
  33 | 
  34 |   const cookies = await page.context().cookies();
  35 |   const authCookie = cookies
  36 |     .filter((cookie) => cookie.name.startsWith("sb-") || cookie.name === "next-auth.session-token")
  37 |     .map((cookie) => `${cookie.name}=${cookie.value}`)
  38 |     .join("; ");
  39 | 
  40 |   fs.writeFileSync(storageStatePath.replace(".json", "-cookie.txt"), authCookie, "utf-8");
  41 | }
  42 | 
  43 | setup("authenticate as User A", async ({ page }) => {
  44 |   await loginAndSave(USER_A_EMAIL, USER_A_PASS, path.join(AUTH_DIR, "user-a.json"), page);
  45 | });
  46 | 
  47 | setup("authenticate as User B", async ({ page }) => {
  48 |   await loginAndSave(USER_B_EMAIL, USER_B_PASS, path.join(AUTH_DIR, "user-b.json"), page);
  49 | });
  50 | 
```